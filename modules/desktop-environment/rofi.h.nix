{
  programs = {
    rofi = {
      enable = true;
      theme = {
        "configuration" = {
          auto-select = true;
          show-icons = false;
          drun-display-format = "{name}";
        };
      };
    };
  };
}
