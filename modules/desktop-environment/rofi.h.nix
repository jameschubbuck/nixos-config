{
  programs.rofi = {
    enable = true;
    # Configure rofi via `extraConfig`, `theme` or `modes` as needed.
    # For example, use `extraConfig` to add raw rasi options or set `theme`.
    # extraConfig = {
    #   lines = "7";
    # };
  };
}
